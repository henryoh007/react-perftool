// Fn
import { OProps } from "./l.d";

function start() {
	// console.log(options);
	if (options.isDevPanelOpened) {
		// console.log("ehre");
		if (options.LIVE_MONITORING) {
			window.doo();
		}
	}
}

export default start;
